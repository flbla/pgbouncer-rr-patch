--- ./include/bouncer.h	2020-03-23 17:54:17.616634752 +0100
+++ ./include/bouncer.h.orig	2020-03-23 17:54:13.224584296 +0100
@@ -87,6 +87,11 @@
 
 extern int cf_sbuf_len;
 
+/* pgbouncer-rr extensions */
+#include "pycall.h"
+#include "route_connection.h"
+#include "rewrite_query.h"
+
 #include "util.h"
 #include "iobuf.h"
 #include "sbuf.h"
@@ -364,6 +369,8 @@
 	usec_t xact_start;	/* xact start moment */
 	usec_t wait_start;	/* waiting start moment */
 
+	char *transaction;
+
 	uint8_t cancel_key[BACKENDKEY_LEN]; /* client: generated, server: remote */
 	PgAddr remote_addr;	/* ip:port for remote endpoint */
 	PgAddr local_addr;	/* ip:port for local endpoint */
@@ -468,6 +475,11 @@
 extern int cf_log_pooler_errors;
 extern int cf_application_name_add_host;
 
+/* pgbouncer-rr extensions */
+extern char *cf_routing_rules_py_module_file;
+extern char *cf_rewrite_query_py_module_file;
+extern char *cf_rewrite_query_disconnect_on_failure;
+
 extern int cf_client_tls_sslmode;
 extern char *cf_client_tls_protocols;
 extern char *cf_client_tls_ca_file;
